name: lu_collab
description: "LU-Collab – Peer-to-Peer Academic Resource & Mentoring Hub"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.4


  # Backend
  supabase_flutter: ^2.5.6

  
  file_picker: ^8.1.2
  image_picker: ^1.1.2

  # UI
  flutter_rating_bar: ^4.0.1
  cached_network_image: ^3.4.1
  intl: ^0.19.0

  # Open URLs / PDFs on device
  url_launcher: ^6.3.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true